## fp32

```plain
一个 FP32 就是 32-bit floating point

所以：
FP32 = 32 b = 4 字节 = 4 B

换算关系是：
1 字节（Byte，B）= 8 位（bit，b）

所以：
32 b ÷ 8 = 4 B

注意大小写很重要：
b = bit，位
B = Byte，字节

比如一个 FP32 数组有 1000 个元素
占用空间就是：
1000 × 4 B = 4000 B ≈ 3.91 KiB

```